# Clerk (required for client sign-in + chat)
VITE_CLERK_PUBLISHABLE_KEY=pk_test_your-key

# Supabase (required for admin auth + inquiry logging)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# AI chat routing (optional — falls back to keyword routing without this)
AI_GATEWAY_API_KEY=
# or
OPENAI_API_KEY=
